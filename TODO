UI/QOL

Toggle sound
Game settings (size, density)
chat log (chat doesnt disappear after refresh)

Auth users
(nickname, historical data, etc)

Play local with bots (no server)
Send "probe" into unexplored regions


