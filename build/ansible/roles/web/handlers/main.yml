---
- name: restart nginx
  service: >
      name=nginx
      enabled=yes
      state=restarted
  sudo: yes

- name: restart uwsgi
  service: >
      name=uwsgi
      enabled=yes
      state=restarted
  sudo: yes
  notify:
    - restart nginx
