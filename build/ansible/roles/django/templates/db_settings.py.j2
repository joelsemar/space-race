DATABASES = {
    'default': {
        'ENGINE': '{{django.db_engine}}',
        'NAME': '{{db.name}}',
        'USER': '{{db.user}}',
        'PASSWORD': '{{db.password}}',
        'HOST': '{{db.host}}',
        'PORT': '{{db.port}}',
    }
}
