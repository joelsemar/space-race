---
- hosts: webservers
  tasks:
    - include: roles/web/tasks/app.yml
    - include: roles/web/tasks/main.yml
  handlers:
    - include: roles/web/handlers/main.yml

  vars_files:
    -  env_vars/${env}.yml
