---
- hosts: localhost
  connection: local
  sudo: yes
  roles:
     - django

  vars_files:
    -  group_vars/default.yml
    -  group_vars/{{env}}.yml
    -  group_vars/encast/default.yml
    -  group_vars/encast/{{env}}.yml
    
- hosts: localhost
  connection: local
  sudo: yes
  roles:
     - django

  vars_files:
    -  group_vars/default.yml
    -  group_vars/{{env}}.yml
    -  group_vars/recommendations/default.yml
    -  group_vars/recommendations/{{env}}.yml
